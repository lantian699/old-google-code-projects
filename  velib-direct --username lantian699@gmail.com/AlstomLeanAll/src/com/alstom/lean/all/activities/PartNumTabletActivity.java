package com.alstom.lean.all.activities;

import android.content.Intent;
import android.os.Bundle;
import android.support.v4.app.FragmentActivity;

import com.alstom.lean.all.R;
import com.alstom.lean.all.fragments.AssistanceRequestFragment;
import com.alstom.lean.all.fragments.DetailSectionFragment;
import com.alstom.lean.all.fragments.MyProjectListFragment;
import com.alstom.lean.all.model3d.Model3DFragment;
import com.alstom.lean.all.fragments.PartNumberDetailFragment;


public class PartNumTabletActivity extends FragmentActivity {//implements PartNumberDetailFragment.Callbacks {
	
/*	
	 private boolean mTwoPane;


	@Override
	    public void onCreate(Bundle savedInstanceState) {
	        super.onCreate(savedInstanceState);
	        setContentView(R.layout.activity_activity_list_part_num);

	        
	        
	        if (findViewById(R.id.activity_detail_container) != null) {
	            mTwoPane = true;
	            ((PartNumberDetailFragment) getSupportFragmentManager()
	                    .findFragmentById(R.id.activity_list_part_num))
	                    .setActivateOnItemClick(true);
	        }
	        
	    }
	    
	 
	 @Override
	    public void onItemSelected(int position) {
	        if (position == 3) {
	            
	            Model3DFragment fragment3D = new Model3DFragment();
	            
	            getSupportFragmentManager().beginTransaction()
	                    .replace(R.id.activity_detail_container, fragment3D)
	                    .commit();
	               
	           

	        } else {
	           
	        }
	    }
	    */

}
